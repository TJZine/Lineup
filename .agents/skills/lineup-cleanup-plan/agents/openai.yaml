interface:
  display_name: "Lineup Cleanup Plan"
  short_description: "Launch the cleanup planner session"
  default_prompt: "Use $lineup-cleanup-plan for this task."
policy:
  allow_implicit_invocation: false
